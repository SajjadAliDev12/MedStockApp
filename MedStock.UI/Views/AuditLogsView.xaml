<UserControl x:Class="MedStock.UI.Views.AuditLogsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             FlowDirection="RightToLeft"
             d:DesignHeight="600" d:DesignWidth="900">

    <UserControl.Resources>
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0,0,1,0"/>
            <Setter Property="BorderBrush" Value="{StaticResource PrimaryDarkBrush}"/>
        </Style>

        <Style TargetType="DataGridRow">
            <Setter Property="Padding" Value="0,2"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{StaticResource PrimaryLightBrush}"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20" Background="{StaticResource BackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="White" Padding="15" CornerRadius="8" Margin="0,0,0,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <WrapPanel Orientation="Horizontal" Grid.Column="1">
                    <StackPanel Margin="0,0,15,5">
                        <TextBlock Text="من تاريخ" Foreground="Gray" FontSize="11" Margin="0,0,0,3"/>
                        <DatePicker SelectedDate="{Binding DateFrom}" Width="120"/>
                    </StackPanel>

                    <StackPanel Margin="0,0,15,5">
                        <TextBlock Text="إلى تاريخ" Foreground="Gray" FontSize="11" Margin="0,0,0,3"/>
                        <DatePicker SelectedDate="{Binding DateTo}" Width="120"/>
                    </StackPanel>

                    <StackPanel Margin="0,0,15,5">
                        <TextBlock Text="المستخدم" Foreground="Gray" FontSize="11" Margin="0,0,0,3"/>
                        <ComboBox ItemsSource="{Binding UsersLookup}" 
                                  DisplayMemberPath="Name" 
                                  SelectedValuePath="Id"
                                  SelectedValue="{Binding SelectedUserId}"
                                  Width="140"/>
                    </StackPanel>

                    <StackPanel Margin="0,0,15,5">
                        <TextBlock Text="نوع الحركة" Foreground="Gray" FontSize="11" Margin="0,0,0,3"/>
                        <ComboBox ItemsSource="{Binding ActionsLookup}" 
                                  SelectedItem="{Binding SelectedAction}"
                                  Width="120"/>
                    </StackPanel>

                    <StackPanel Margin="0,0,15,5">
                        <TextBlock Text="بحث في التفاصيل" Foreground="Gray" FontSize="11" Margin="0,0,0,3"/>
                        <TextBox Text="{Binding SearchText}" Width="160" Padding="5"/>
                    </StackPanel>
                </WrapPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Bottom" Margin="10,0,0,5">
                    <Button Content="بحث" Command="{Binding SearchCommand}" 
                            Width="80" Height="30" Background="{StaticResource PrimaryBrush}" Foreground="White" FontWeight="Bold" Margin="5,0"/>

                    <Button Content="مسح" Command="{Binding ClearFiltersCommand}" 
                            Width="60" Height="30" Background="{StaticResource SecondaryBrush}"/>
                </StackPanel>
            </Grid>
        </Border>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Logs}" AutoGenerateColumns="False" 
                  IsReadOnly="True" HeadersVisibility="Column" GridLinesVisibility="Horizontal"
                  Background="White" BorderThickness="0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="الرقم" Binding="{Binding AuditLogId}" Width="60"/>
                <DataGridTextColumn Header="التاريخ/الوقت" Binding="{Binding OccurredAt, StringFormat='yyyy-MM-dd HH:mm'}" Width="130"/>
                <DataGridTextColumn Header="المستخدم" Binding="{Binding UserName}" Width="120" FontWeight="SemiBold"/>
                <DataGridTextColumn Header="النوع" Binding="{Binding ActionType}" Width="100"/>
                <DataGridTextColumn Header="الشاشة/الكيان" Binding="{Binding EntityName}" Width="120"/>
                <DataGridTextColumn Header="الملخص" Binding="{Binding Summary}" Width="*"/>
                <DataGridTextColumn Header="IP" Binding="{Binding IpAddress}" Width="100" Foreground="Gray"/>
            </DataGrid.Columns>
        </DataGrid>

        <Grid Grid.Row="2" Margin="0,10">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="السابق" Command="{Binding PrevPageCommand}" 
                        Width="80" Margin="5,0"/>

                <Border Background="#F5F5F5" CornerRadius="4" Margin="10,0" Padding="15,5">
                    <TextBlock Text="{Binding PageInfo}" FontWeight="Bold" VerticalAlignment="Center"/>
                </Border>

                <Button Content="التالي" Command="{Binding NextPageCommand}" 
                        Width="80" Margin="5,0"/>
            </StackPanel>
        </Grid>

        <Border Grid.Row="3" Padding="10" CornerRadius="5" Background="#E3F2FD" BorderBrush="#BBDEFB" BorderThickness="1">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="ℹ" Foreground="{StaticResource PrimaryBrush}" Margin="0,0,8,0"/>
                <TextBlock Text="{Binding StatusMessage}" Foreground="{StaticResource PrimaryDarkBrush}" FontWeight="SemiBold"/>

                <TextBlock Text="جاري التحميل..." Foreground="OrangeRed" Margin="20,0,0,0" 
                           Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}"/>
            </StackPanel>
        </Border>

    </Grid>
</UserControl>