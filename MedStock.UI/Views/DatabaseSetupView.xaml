<Window x:Class="MedStock.UI.Views.DatabaseSetupView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="إعدادات الاتصال بالسيرفر" Height="550" Width="450" 
        WindowStartupLocation="CenterScreen" FlowDirection="RightToLeft"
        ResizeMode="NoResize" Background="#F5F5F5">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" BorderBrush="#DDD" BorderThickness="0,0,0,1" Padding="0,0,0,15" Margin="0,0,0,15">
            <StackPanel>
                <TextBlock Text="إعداد قاعدة البيانات" FontSize="22" FontWeight="Bold" Foreground="#333"/>
                <TextBlock Text="يرجى إدخال تفاصيل الاتصال بخادم SQL Server." FontSize="12" Foreground="Gray" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <TextBlock Text="اسم السيرفر (Server Instance):" FontWeight="SemiBold"/>
                <TextBox Text="{Binding Server}" Height="35" Margin="0,5,0,15" Padding="5" VerticalContentAlignment="Center"/>

                <TextBlock Text="اسم قاعدة البيانات:" FontWeight="SemiBold"/>
                <TextBox Text="{Binding Database}" Height="35" Margin="0,5,0,15" Padding="5" VerticalContentAlignment="Center"/>

                <CheckBox IsChecked="{Binding UseSqlAuth}" Margin="0,0,0,10">
                    <TextBlock Text="استخدام اسم مستخدم وكلمة مرور (SQL Authentication)" FontSize="13"/>
                </CheckBox>

                <StackPanel Visibility="{Binding UseSqlAuth, Converter={StaticResource BooleanToVisibilityConverter}}" 
                            Background="White" Margin="0,0,0,15" >
                    <Border BorderBrush="#DDD" BorderThickness="1" CornerRadius="4" Padding="10">
                        <StackPanel>
                            <TextBlock Text="اسم المستخدم (User ID):" Margin="0,0,0,5"/>
                            <TextBox Text="{Binding Username}" Height="30" Margin="0,0,0,10" Padding="2"/>

                            <TextBlock Text="كلمة المرور (Password):" Margin="0,0,0,5"/>
                            <PasswordBox x:Name="TxtPassword" PasswordChar="●" Height="30" PasswordChanged="TxtPassword_PasswordChanged"/>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <TextBlock Text="نوع النشاط:" FontWeight="SemiBold"/>
                <ComboBox SelectedValue="{Binding ActivityType}" SelectedValuePath="Tag" Height="35" Margin="0,5,0,5" Padding="5" VerticalContentAlignment="Center">
                    <ComboBoxItem Content="مستشفى (Hospital)" Tag="Hospital" IsSelected="True"/>
                    <ComboBoxItem Content="مذخر أدوية / جملة (Wholesale)" Tag="Wholesale"/>
                </ComboBox>
            </StackPanel>
        </ScrollViewer>

        <StackPanel Grid.Row="2" Margin="0,15,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" Content="اختبار الاتصال" Command="{Binding TestCommand}" Height="40" Background="#E0E0E0" BorderThickness="0"/>
                <Button Grid.Column="2" Content="حفظ وتشغيل" Command="{Binding SaveCommand}" Height="40" Background="#007ACC" Foreground="White" FontWeight="Bold" BorderThickness="0"/>
            </Grid>

            <TextBlock Text="{Binding StatusMessage}" Margin="0,10,0,0" Foreground="#D32F2F" TextWrapping="Wrap" HorizontalAlignment="Center" FontWeight="Medium"/>

            <ProgressBar IsIndeterminate="True" Height="2" Margin="0,5,0,0" Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}"/>
        </StackPanel>
    </Grid>
</Window>